define(function(require) {	
	var Adapt = require('coreJS/adapt');
	Adapt.on('app:dataReady', function(view) {
    	// 'view' is the current view triggering the event
    	//$(".navigation-logo").css("display","none").delay(800).fadeIn(1000);
	})

});